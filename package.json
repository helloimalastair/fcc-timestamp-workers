{
  "scripts": {
    "build": "esbuild src/index.mjs --bundle --loader:.html=text --loader:.css=text --minify --format=esm --target=es2020 --sourcemap --outfile=dist/worker.mjs",
    "test": "miniflare"
  },
  "dependencies": {
    "esbuild": "^0.14.14",
    "itty-router": "^2.4.10",
    "miniflare": "^2.2.0",
    "wrangler": "^0.0.13"
  }
}
